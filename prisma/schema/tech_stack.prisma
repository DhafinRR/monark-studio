model TechStack {
    id        String             @id @default(uuid())
    name      String             @unique
    icon_url  String?            // URL icon SVG/PNG
    color_hex String?            @default("#3b82f6") // Warna untuk pills (hex)
    projects  PortfolioProject[]

    @@map("tech_stack")
}
